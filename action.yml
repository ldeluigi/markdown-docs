name: 'Markdown Docs'
author: ldeluigi
description: 'Generates a documentation website from Markdown files with many extensions: https://github.com/ldeluigi/markdown-docs#readme'
branding:
  icon: 'file-text'
  color: 'gray-dark'
inputs:
  src:
    description: 'Source path. Default: .'
    required: true
    default: '.'
  dst:
    description: 'Output path. Default: docs'
    required: true
    default: 'docs'
  language:
    description: 'Language selector for mkdocs search feature and more. Default: en'
    required: false
    default: 'en'
runs:
  using: 'docker'
  image: 'docker://deloo/markdown-docs'
  env:
    LANGUAGE: ${{ inputs.language }}
  args:
    - ${{ inputs.src }}
    - ${{ inputs.dst }}
